{
    "version": 2,
    "buildCommand": "bash build.sh",
    "installCommand": "echo 'Pulando install padrão, usando build.sh'",
        "rewrites": [
            {
                "source": "/(.*)",
                "destination": "/dist/server.js"
            }
        ]
}