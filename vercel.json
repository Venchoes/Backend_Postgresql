{
    "version": 2,
    "functions": {
        "api/vercel.ts": {
            "runtime": "nodejs20.x",
            "memory": 512,
            "maxDuration": 10
        }
    },
    "routes": [
            { "src": "^/health$", "dest": "api/health.ts" },
        { "src": "/(.*)", "dest": "api/vercel.ts" }
    ]
}